<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PrivateTube</title>
    <link rel="manifest" href="~/manifest.json" />
    <meta name="theme-color" content="#0f0f0f">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #0f0f0f;
            --header-bg: #0f0f0f;
            --sidebar-bg: #0f0f0f;
            --text-color: #fff;
            --hover-bg: #272727;
            --search-bg: #121212;
            --border-color: #303030;
            --card-bg: #1e1e1e;
            /* New variable for cards */
            --accent: #cc0000;
        }

        .light-theme {
            --bg-color: #f9f9f9;
            --header-bg: #ffffff;
            --sidebar-bg: #ffffff;
            --text-color: #0f0f0f;
            --hover-bg: #f2f2f2;
            --search-bg: #f0f0f0;
            --border-color: #e5e5e5;
            --card-bg: #ffffff;
            --accent: #cc0000;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding-top: 56px;
            overflow-x: hidden;
            transition: background-color 0.3s, color 0.3s;
        }

        /* Header logic needs update for light theme background */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            height: 56px;
            background: var(--header-bg);
            /* ... */
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 16px;
            z-index: 2000;
            box-sizing: border-box;
            transition: background-color 0.3s;
        }

        /* ... skipped unchanged ... */

        /* Sidebar Item - Reduced Padding */
        .sidebar-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            padding: 8px 0;
            /* Reduced from 16px */
            color: var(--text-color);
            /* Use var */
            text-decoration: none;
            border-radius: 10px;
            font-size: 10px;
            gap: 4px;
            white-space: nowrap;
            overflow: hidden;
            flex-shrink: 0;
            margin-bottom: 2px;
            width: 64px;
        }

        .sidebar-item:hover {
            background: var(--hover-bg);
        }

        .start {
            display: flex;
            align-items: center;
        }

        .center {
            flex: 1;
            display: flex;
            justify-content: center;
            max-width: 720px;
            margin: 0 40px;
        }

        .end {
            display: flex;
            align-items: center;
            gap: 10px;
            min-width: 225px;
            justify-content: flex-end;
        }

        .menu-btn {
            background: none;
            border: none;
            color: #fff;
            padding: 8px;
            border-radius: 50%;
            cursor: pointer;
            margin-right: 16px;
        }

        .menu-btn:hover {
            background: #272727;
        }

        .logo {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: #fff;
            font-size: 20px;
            /* YouTube default usually 20-22px */
            font-family: 'Roboto Condensed', sans-serif;
            letter-spacing: -1px;
            font-weight: 600;
        }

        .logo-icon {
            width: 90px;
            height: 20px;
            font-family: inherit;
        }

        /* Search */
        .search-box {
            display: flex;
            width: 100%;
            margin-left: 40px;
            max-width: 600px;
        }

        .search-input {
            width: 100%;
            background: #121212;
            border: 1px solid #303030;
            border-right: none;
            color: #fff;
            padding: 0 16px;
            height: 40px;
            font-size: 16px;
            border-radius: 40px 0 0 40px;
            outline: none;
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.3);
        }

        .search-input:focus {
            border-color: #1c62b9;
        }

        .search-btn {
            background: #222;
            border: 1px solid #303030;
            color: #fff;
            padding: 0 20px;
            height: 40px;
            border-radius: 0 40px 40px 0;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .search-btn:hover {
            background: #303030;
        }

        /* Auth Buttons */
        .login-btn {
            display: flex;
            align-items: center;
            border: 1px solid #303030;
            /* YouTube uses darker border now or transparent */
            color: #3ea6ff;
            background: transparent;
            padding: 5px 15px;
            /* Pill shape */
            border-radius: 18px;
            text-decoration: none;
            font-weight: 500;
            font-size: 14px;
            gap: 6px;
            border-color: rgba(255, 255, 255, 0.1);
        }

        .login-btn:hover {
            background: rgba(62, 166, 255, 0.1);
            border-color: transparent;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            background: #555;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            text-decoration: none;
            color: #fff;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            left: 0;
            top: 56px;
            bottom: 0;
            width: 72px;
            background: var(--sidebar-bg);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 4px;
            z-index: 1000;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            left: 0;
            top: 56px;
            bottom: 0;
            width: 72px;
            background: var(--sidebar-bg);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 4px;
            z-index: 1000;
            transition: width 0.1s;
            /* Smooth transition */
        }

        .sidebar.expanded {
            width: 240px;
            align-items: flex-start;
            padding-left: 12px;
            background: #0f0f0f;
            box-shadow: 4px 0 8px rgba(0, 0, 0, 0.3);
        }

        /* Dropdown Items */
        .dropdown-item {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 10px 16px;
            color: #f1f1f1;
            text-decoration: none;
            font-size: 14px;
        }

        .dropdown-item:hover {
            background: #3e3e3e;
        }

        .dropdown-item svg {
            width: 24px;
            height: 24px;
            fill: #909090;
        }

        .sidebar-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            padding: 16px 0;
            color: #fff;
            text-decoration: none;
            border-radius: 10px;
            font-size: 10px;
            gap: 6px;
            white-space: nowrap;
            overflow: hidden;
            flex-shrink: 0;
            /* Added padding refinement */
            margin-bottom: 4px;
            width: 64px;
        }

        .sidebar-item:hover {
            background: var(--hover-bg);
        }

        .sidebar-item svg {
            width: 24px;
            height: 24px;
            fill: currentColor;
            flex-shrink: 0;
            margin-bottom: 0;
        }

        /* Expanded State items */
        .sidebar.expanded .sidebar-item {
            flex-direction: row;
            justify-content: flex-start;
            font-size: 14px;
            padding: 10px 12px;
            gap: 24px;
            width: 90%;
            height: 48px;
        }

        .sidebar.expanded .sidebar-item svg {
            margin-right: 0;
        }

        /* Main Content */
        .main-content {
            margin-left: 72px;
            padding: 24px;
        }

        /* Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 16px;
            row-gap: 40px;
        }

        .video-card {
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: flex;
            flex-direction: column;
        }

        .thumbnail-container {
            position: relative;
            width: 100%;
            aspect-ratio: 16/9;
            border-radius: 12px;
            overflow: hidden;
            background: #222;
        }

        .thumbnail {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.2s;
        }

        .video-card:hover .thumbnail {
            transform: scale(1.02);
            transition-delay: 0.1s;
        }

        .details {
            display: flex;
            margin-top: 12px;
            gap: 12px;
        }

        .channel-icon {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: #555;
            flex-shrink: 0;
        }

        .meta {
            display: flex;
            flex-direction: column;
            width: 100%;
        }

        .title {
            font-weight: 500;
            font-size: 16px;
            line-height: 1.4;
            margin-bottom: 4px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .channel-name {
            font-size: 14px;
            color: #aaa;
        }

        .stats {
            font-size: 14px;
            color: #aaa;
        }

        @@media (max-width: 792px) {
            .sidebar {
                display: none;
            }

            .main-content {
                margin-left: 0;
                padding-bottom: 72px; /* Space for bottom nav */
            }

            .center {
                display: none;
            }

            .bottom-nav {
                display: flex;
            }
        }

        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 56px;
            background: var(--header-bg);
            border-top: 1px solid var(--border-color);
            display: none;
            justify-content: space-around;
            align-items: center;
            z-index: 2000;
        }

        .bottom-nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: var(--text-color);
            text-decoration: none;
            font-size: 10px;
            gap: 4px;
            opacity: 0.7;
        }

        .bottom-nav-item.active {
            opacity: 1;
        }

        .bottom-nav-item svg {
            width: 24px;
            height: 24px;
            fill: currentColor;
        }
    </style>
</head>

<body>
    <header>
        <div class="start">
            <button class="menu-btn" onclick="toggleSidebar()">
                <svg viewBox="0 0 24 24" preserveAspectRatio="xMidYMid meet" focusable="false"
                    style="width: 24px; height: 24px; display: block; fill: #fff;">
                    <g>
                        <path d="M21 6H3V5h18v1zm0 5H3v1h18v-1zm0 6H3v1h18v-1z"></path>
                    </g>
                </svg>
            </button>
            <a href="/" class="logo" title="PrivateTube Home" style="display: flex; align-items: center; text-decoration: none; gap: 4px;">
                <svg viewBox="0 0 24 24" style="width: 32px; height: 32px; fill: #FF0000;">
                    <path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 4-8 4z"/>
                </svg>
                <span style="color: var(--text-color); font-weight: bold; font-size: 18px; letter-spacing: -0.5px; font-family: 'Roboto', 'Arial', sans-serif;">PrivateTube</span>
            </a>
        </div>
        <div class="center">
            <form action="/Home/Search" method="get" class="search-box">
                <input type="text" name="query" class="search-input" placeholder="Search" value="@ViewData["Query"]">
                <button type="submit" class="search-btn">
                    <svg viewBox="0 0 24 24" style="width: 24px; height: 24px; fill: #fff;">
                        <path
                            d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z">
                        </path>
                    </svg>
                </button>
            </form>
        </div>
        <div class="end">
            @if (User.Identity!.IsAuthenticated)
            {
                <div class="user-menu-container" style="position: relative;">
                    <button class="user-avatar-btn" onclick="toggleUserMenu()"
                        style="border: none; background: none; cursor: pointer; padding: 0;">
                        <div class="user-avatar" title="@User.Identity.Name">
                            @User.Identity.Name!.Substring(0,1).ToUpper()
                        </div>
                    </button>

                    <div id="userDropdown" class="dropdown-content"
                        style="display: none; position: absolute; top: 40px; right: 0; background: #282828; min-width: 200px; border-radius: 12px; box-shadow: 0 4px 8px rgba(0,0,0,0.5); z-index: 2001; overflow: hidden;">
                        <div
                            style="padding: 16px; border-bottom: 1px solid #3e3e3e; display: flex; gap: 12px; align-items: center;">
                            <div class="user-avatar" style="width: 40px; height: 40px;">
                                @User.Identity.Name!.Substring(0,1).ToUpper()</div>
                            <div style="display: flex; flex-direction: column;">
                                <span style="font-weight: 500; font-size: 16px;">@User.Identity.Name</span>
                                <span style="color: #aaa; font-size: 12px;">Manage your account</span>
                            </div>
                        </div>

                        <div style="padding: 8px 0;">
                            <a asp-controller="Studio" asp-action="Index" class="dropdown-item">
                                <svg viewBox="0 0 24 24">
                                    <path d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"></path>
                                </svg>
                                Your Channel
                            </a>
                            @if (User.IsInRole("Admin"))
                            {
                                <a asp-controller="Admin" asp-action="Index" class="dropdown-item">
                                    <svg viewBox="0 0 24 24">
                                        <path
                                            d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z">
                                        </path>
                                    </svg>
                                    Admin Dashboard
                                </a>
                                <a href="/Admin/CreateVideo" class="dropdown-item">
                                    <svg viewBox="0 0 24 24">
                                        <path d="M14 13h-3v3H9v-3H6v-2h3V8h2v3h3v2zM4 6h16v12H4zM2 4v16h20V4H2z"></path>
                                    </svg>
                                    Create Video (Admin)
                                </a>
                            }
                            <a href="#" onclick="toggleTheme(); return false;" class="dropdown-item">
                                <svg viewBox="0 0 24 24"><path d="M20 8.69V4h-4.69L12 .69 8.69 4H4v4.69L.69 12 4 15.31V20h4.69L12 23.31 15.31 20H20v-4.69L23.31 12 20 8.69zM12 18c-3.31 0-6-2.69-6-6s2.69-6 6-6 6 2.69 6 6-6.69 6-6 6zm0-10c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4z"></path></svg>
                                Appearance: <span id="themeLabel">Dark</span>
                            </a>
                            <div style="border-top: 1px solid #3e3e3e; margin: 8px 0;"></div>
                            <a asp-controller="Account" asp-action="Logout" class="dropdown-item">
                                <svg viewBox="0 0 24 24">
                                    <path
                                        d="M10.09 15.59L11.5 17l5-5-5-5-1.41 1.41L12.67 11H3v2h9.67l-2.58 2.59zM19 3H5c-1.11 0-2 .9-2 2v4h2V5h14v14H5v-4H3v4c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z">
                                    </path>
                                </svg>
                                Sign Out
                            </a>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <a asp-controller="Account" asp-action="Login" class="login-btn">
                    <div
                        style="border: 1px solid currentColor; border-radius: 50%; width: 20px; height: 20px; display: flex; align-items: center; justify-content: center; margin-right: 6px;">
                        <svg viewBox="0 0 24 24" style="width: 16px; height: 16px; fill: currentColor;">
                            <path
                                d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z">
                            </path>
                        </svg>
                    </div>
                    Sign in
                </a>
            }
        </div>
    </header>

    <div class="sidebar">
        <div style="flex-grow: 1; width: 100%;">
            <a href="/" class="sidebar-item">
                <svg viewBox="0 0 24 24">
                    <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"></path>
                </svg>
                <span>Home</span>
            </a>

            @if (User.Identity!.IsAuthenticated)
            {
                <div style="border-top: 1px solid #303030; margin: 12px 0 8px 0; width: 100%;"></div>

                <a asp-controller="Studio" asp-action="Index" class="sidebar-item" title="Your Channel">
                    <svg viewBox="0 0 24 24">
                        <path d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"></path>
                    </svg>
                    <span>Your Channel</span>
                </a>

                @* Ideally we fetch user playlists here via ViewComponent, but for now linking to Studio Index is fine *@
            }

            @if (User.IsInRole("Admin"))
            {
                <div style="border-top: 1px solid #303030; margin: 12px 0 8px 0; width: 100%;"></div>
                <a asp-controller="Admin" asp-action="Index" class="sidebar-item">
                    <svg viewBox="0 0 24 24">
                        <path
                            d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z">
                        </path>
                    </svg>
                    <span>Admin</span>
                </a>
            }
        </div>
    </div>

    <div class="main-content">
        @RenderBody()
    </div>

    <div class="bottom-nav">
        <a href="/" class="bottom-nav-item @(Context.Request.Path == "/" ? "active" : "")">
            <svg viewBox="0 0 24 24"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"></path></svg>
            <span>Home</span>
        </a>
        <a asp-controller="Home" asp-action="Playlist" class="bottom-nav-item @(Context.Request.Path.Value!.Contains("/Playlist") ? "active" : "")">
            <svg viewBox="0 0 24 24"><path d="M4 10h12v2H4zm0-4h12v2H4zm0 8h8v2H4zm10 0v6l5-3z"></path></svg>
            <span>Playlists</span>
        </a>
        <a asp-controller="Studio" asp-action="Index" class="bottom-nav-item @(Context.Request.Path.Value!.Contains("/Studio") ? "active" : "")">
            <svg viewBox="0 0 24 24"><path d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"></path></svg>
            <span>Studio</span>
        </a>
        @if (User.IsInRole("Admin"))
        {
            <a asp-controller="Admin" asp-action="Index" class="bottom-nav-item @(Context.Request.Path.Value!.Contains("/Admin") ? "active" : "")">
                <svg viewBox="0 0 24 24"><path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z"></path></svg>
                <span>Admin</span>
            </a>
        }
    </div>

    <script>
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('/sw.js');
            });
        }
        function toggleSidebar() {
            const sidebar = document.querySelector('.sidebar');
            sidebar.classList.toggle('expanded');
        }
        function toggleUserMenu() {
            const dropdown = document.getElementById('userDropdown');
            if (dropdown.style.display === 'none' || dropdown.style.display === '') {
                dropdown.style.display = 'block';
            } else {
                dropdown.style.display = 'none';
            }
        }

        // Close dropdown when clicking outside
        window.onclick = function (event) {
            if (!event.target.closest('.user-menu-container')) {
                const dropdown = document.getElementById('userDropdown');
                if (dropdown) dropdown.style.display = 'none';
            }
        }
        // Theme Logic
        function toggleTheme() {
            document.body.classList.toggle('light-theme');
            const isLight = document.body.classList.contains('light-theme');
            localStorage.setItem('theme', isLight ? 'light' : 'dark');
            const lbl = document.getElementById('themeLabel');
            if(lbl) lbl.innerText = isLight ? 'Light' : 'Dark';
        }
        
        // Init Theme
        (function() {
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme === 'light') {
                document.body.classList.add('light-theme');
                const lbl = document.getElementById('themeLabel');
                if(lbl) lbl.innerText = 'Light';
            }
        })();
    </script>
</body>

</html>
